import { RingAction } from "./RingAction";
import { RingMetadata } from "./RingMetadata";
import { RingPlayer } from "./RingPlayer";
import { RingRoot } from "./RingRoot";

export const RingBox = {
    Root: RingRoot,
    Player: RingPlayer,
    Metadata: RingMetadata,
    Action: RingAction
};